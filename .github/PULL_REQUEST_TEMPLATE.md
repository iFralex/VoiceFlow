## Summary

<!-- What does this PR do? Link the plan section it implements. -->

Implements: <!-- e.g. Plan 01 — Task 5: Source folder structure -->

## Changes

<!-- Brief bullet list of what changed -->

-

## Plan-completion checklist

<!-- Tick every item that applies to this PR -->

- [ ] All validation commands pass locally (`pnpm lint`, `pnpm format:check`, `pnpm typecheck`, `pnpm test`, `pnpm build`)
- [ ] New code has unit tests or existing tests updated
- [ ] `.env.example` updated if new environment variables were added
- [ ] Plan file checkboxes marked `[x]` for every item completed
- [ ] No secrets, credentials, or `.env.local` files committed
- [ ] PR title follows Conventional Commits (`feat:`, `fix:`, `chore:`, etc.)

## Test plan

<!-- How was this tested? -->

- [ ] `pnpm test` passes
- [ ] Manual smoke test performed (describe steps if non-trivial)

## Notes for reviewer

<!-- Anything the reviewer should know: trade-offs, follow-up items, risks -->
